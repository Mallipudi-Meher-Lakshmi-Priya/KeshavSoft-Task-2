# KeshavSoft Internship - Task 2

This project demonstrates a professional frontend workflow using Nunjucks for templating and Vite for building and bundling.

## 🛠️ Mandatory Tech Stack Implementation
- **Templating Engine:** Nunjucks (used for layout inheritance via `layout.njk`)
- **Bundler:** Vite (used for compiling source templates into production HTML)
- **Framework:** Bootstrap 5 (used for responsive UI components)

## 📂 Folder Structure
- `/src/templates`: Source templates (`layout.njk`, `index.html`, etc.)
- `/dist`: Production-ready compiled HTML output (generated by Vite)
- `vite.config.js`: Configuration for Nunjucks and build paths
- `package.json`: Project metadata and dependencies

## 🚀 Commands
Use these commands to interact with the project:

- **Install Dependencies:**
    npm install
  
- **Build Production Files:**
    npm run build

- **Preview Production Build:**
    npx vite preview
